<div class="page-header">
  <div class="container">
    <a href="/handbook" class="back-link"><i class="fas fa-arrow-left"></i> Quay lại cẩm nang</a>
    <h2>Tất cả sự kiện</h2>
    <p class="subtitle">Khám phá các sự kiện sôi động đã, đang và sẽ diễn ra.</p>
  </div>
</div>

<div class="container page-content">
  <div class="row">
    <% if (events && events.length > 0) { %>
      <% events.forEach(event => { %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card h-100">
            <img src="<%= event.imageUrl || '/images/default-event-card.jpg' %>" class="card-img-top" alt="<%= event.title %>" style="height: 200px; object-fit: cover;">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= event.title %></h5>
              <p class="card-text text-muted flex-grow-1"><%= event.shortDescription %></p>
              <p class="card-text"><small class="text-muted"><i class="fas fa-calendar-alt"></i> <%= new Date(event.startDate).toLocaleDateString('vi-VN') %></small></p>
              <a href="/handbook/event/<%= event._id %>" class="btn btn-primary mt-auto">Xem chi tiết</a>
            </div>
          </div>
        </div>
      <% }); %>
    <% } else { %>
      <div class="col">
        <p>Hiện chưa có sự kiện nào được công bố.</p>
      </div>
    <% } %>
  </div>
</div>
