<div class="container my-5">
  <div class="page-header text-center mb-5 text-danger">
    <h1><i class="fas fa-heartbeat me-2"></i>Nguồn hỗ trợ khẩn cấp</h1>
    <p class="lead">Nếu bạn đang gặp khủng hoảng, đừng ngần ngại tìm kiếm sự giúp đỡ. Bạn không đơn độc.</p>
  </div>

  <div class="row justify-content-center">
    <div class="col-lg-8">
      <% if (crisisResources && crisisResources.length > 0) { %>
        <% crisisResources.forEach(resource => { %>
          <div class="card text-center mb-4">
            <div class="card-header">
              <h4 class="mb-0"><%= resource.name %></h4>
            </div>
            <div class="card-body">
              <h5 class="card-title display-6"><i class="fas fa-phone-alt me-2"></i><%= resource.phone %></h5>
              <p class="card-text"><%= resource.description %></p>
            </div>
            <div class="card-footer text-muted">
              Hoạt động: <%= resource.available %>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <p class="text-center">Không có thông tin về nguồn hỗ trợ.</p>
      <% } %>
    </div>
  </div>
</div>
