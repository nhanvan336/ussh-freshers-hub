<div class="sticky-top" style="top: 20px;">
    <!-- Nút Tạo bài đăng -->
    <div class="d-grid mb-4">
        <% if (locals.user) { %>
            <!-- Nếu người dùng đã đăng nhập, hiển thị nút này -->
            <a href="/forum/create" class="btn btn-primary btn-lg">
                <i class="fas fa-plus-circle"></i> Tạo bài đăng
            </a>
        <% } else { %>
            <!-- Nếu chưa đăng nhập, hiển thị nút này -->
            <a href="/login" class="btn btn-primary btn-lg">
                <i class="fas fa-sign-in-alt"></i> Đăng nhập để đăng bài
            </a>
        <% } %>
    </div>

    <!-- [XÓA] Phần thống kê đã được loại bỏ -->
</div>

