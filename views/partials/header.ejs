<header class="header">
    <div class="container">
        <nav class="navbar" role="navigation" aria-label="Main navigation">
            <!-- Logo and Brand - ĐÃ CẬP NHẬT -->
            <a href="/" class="navbar-brand">
                <!-- Thay thế bằng logo USSH -->
                <img src="/images/logo-ussh.png" alt="Logo USSH" class="brand-logo">
                <!-- Tên trang web -->
                <h1 class="brand-name">USSH Freshers' Hub</h1>
            </a>
            
            <!-- Mobile Menu Toggle (Giữ nguyên) -->
            <button class="mobile-menu-toggle" aria-label="Toggle navigation menu" aria-expanded="false">
                <span class="hamburger-line"></span>
                <span class="hamburger-line"></span>
                <span class="hamburger-line"></span>
            </button>
            
            <!-- Navigation Menu (Giữ nguyên cấu trúc) -->
            <div class="navbar-menu">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a href="/" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'home' ? 'active' : '' %>">
                            <i class="fas fa-home"></i>
                            <span>Trang chủ</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/learning-hub" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'learning-hub' ? 'active' : '' %>">
                            <i class="fas fa-book-open"></i>
                            <span>Học tập</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/forum" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'forum' ? 'active' : '' %>">
                            <i class="fas fa-users"></i>
                            <span>Diễn đàn</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/wellness" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'wellness' ? 'active' : '' %>">
                            <i class="fas fa-heart"></i>
                            <span>Sức khỏe</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/handbook" class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'handbook' ? 'active' : '' %>">
                            <i class="fas fa-map"></i>
                            <span>Cẩm nang</span>
                        </a>
                    </li>
                </ul>
                
                <!-- User Actions (Giữ nguyên logic) -->
                <div class="navbar-actions">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <!-- Logged in user -->
                        <div class="user-menu dropdown">
                            <!-- ... (Toàn bộ code menu người dùng của bạn giữ nguyên) ... -->
                        </div>
                    <% } else { %>
                        <!-- Guest user -->
                        <div class="auth-buttons">
                            <a href="/login" class="btn btn-outline">Đăng nhập</a>
                            <a href="/register" class="btn btn-primary">Đăng ký</a>
                        </div>
                    <% } %>
                </div>
            </div>
        </nav>
    </div>
</header>
